import { ContentPlaceholder, ContentPlaceholderLabel } from "@pstdio/ui";

export const Settings = () => (
  <ContentPlaceholder>
    <ContentPlaceholderLabel>Settings</ContentPlaceholderLabel>
  </ContentPlaceholder>
);
